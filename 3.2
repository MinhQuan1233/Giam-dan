#include<stdio.h>
#include<conio.h>

int main()
{
	int a[100];
	int n;
	int dem = 0;
	printf(" Nhap vao phan tu n: ");
	scanf("%d", &n);
	do
	{
		if(n <= 0)
		printf("\n NHap lai: ");
		
	}while(n <= 0);
	for(int i = 0; i < n; i++)
	{
		printf("\nNhap a[%d]: ", i);
		scanf("%d", &a[i]);
		
	}
	int giam;
	int i;
	for(int i = 0; i < n + 1; i++)
		
	for(int j = i + 1; j < n; j++){
	if(a[i] < a[j])
	{
		giam = a[i];
		a[i] = a[j];
		a[j] = giam;
	}
  }


   printf("\nMang da sap xep lai: ");
   for(int i = 0; i < n; i++)
{
	printf("%5d", a [i]);
}
}
